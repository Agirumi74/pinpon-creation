---
// Cursor.astro: Custom animated cursor component
---
<div id="cursor"></div>
<script is:inline>
  import('/src/scripts/ultimate-cursor.js').then(mod => mod.initUltimateCursor());
</script>
<style>
#cursor {
  position: fixed;
  left: 0; top: 0;
  width: 38px; height: 38px;
  pointer-events: none;
  z-index: 9999;
  opacity: 1;
  transition: opacity 0.2s;
}
.pinpon-cursor {
  width: 100%;
  height: 100%;
  display: block;
}
.glow-light {
  filter: drop-shadow(0 0 12px #fff8) drop-shadow(0 0 24px #fff6);
}
.glow-dark {
  filter: drop-shadow(0 0 12px #232a3d88) drop-shadow(0 0 24px #232a3d66);
}
.glow-blue {
  filter: drop-shadow(0 0 12px #1da1f2cc) drop-shadow(0 0 24px #1da1f288);
}
#cursor.clicked {
  transform: scale(0.92);
  transition: transform 0.08s;
}
#cursor.hovered {
  filter: brightness(1.2) drop-shadow(0 0 8px var(--accent-red));
}
</style>
